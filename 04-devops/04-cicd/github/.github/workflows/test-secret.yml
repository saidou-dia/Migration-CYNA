name: Test Secret Azure

on:
  push:
    branches:
      - main

jobs:
  test-azure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: List Azure Resource Groups
        run: az group list -o table
